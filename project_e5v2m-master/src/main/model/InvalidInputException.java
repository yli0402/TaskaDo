package model;

//EFFECTS: throw exception when the user inputs invalid value(negative value of dueInDays).
public class InvalidInputException extends Exception {
    public InvalidInputException() {
    }
}
